-- CreateTable
CREATE TABLE "Settings" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "heading" TEXT NOT NULL,
    "headingColor" TEXT NOT NULL,
    "amount" INTEGER NOT NULL,
    "sliderColor" TEXT NOT NULL
    "shop" TEXT NOT NULL,
);
